// to recieve the data, then map it (create new array) and send to displayList to display
//          {JSON.stringify(list_data)}
//             {JSON.stringify(data)}
const ListDisplay = ( { list_data, data }) => {
    return (
        <div>

      </div>
    )}
export default ListDisplay;
